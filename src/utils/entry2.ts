/**
 * Why directory/file representedor named as "entry"?
 * The terminology comes from Directory Entry, it use to represent single point
 * on a directory whether it is a file or directory.
 *
 *
 */
export function Entry() {}
